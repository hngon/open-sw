반드시 push전에 pull부터 하고 push하기
// 0317 수업
서로 다른 사용자가 같은 시기에 병합할 때 충돌이 일어나면 3way merge 방식으로 구분
에를 들어 version 1, 2, 3, 4 중 사용자1이 3에서 수정 후 병합, 사용자2가 4에서 수정 후 병합하면(같은 시기에 병합한다 가정.) 
base code는 version3에서 끌어와 수정한 사용자1이 기준임.
해결하려면 사용자2가 base code를 사용자1이 병합한 코드를 최신화 후 서로 상의, 코드 최신화 하고 push하기(pull request 활용)
그럼 사용자1이 병합한 코드에 사용자2의 코드가 합쳐짐.

오픈소스애서는 원본 저장소를 복사한 복제 저장소(나만의 저장소)에 push하고 원본 저장소 주인에게 자신의 저장소인 복제 저장소에서 pull 요구(fork 방식), 
원본 저장소 주인이 확인 후 merge해주면 성공

// 실습 내용 
깃헙 저장소 url을 협업자에게 전송, 협업자는 수정하려면 fork해서 나만의 저장소를 만듦(만약 원본 저장소가 코드가 바뀌면 synkfork해서 최신화),
협업자가 commit 후 Pull requests하면 원본 저장소에게 변경사항이 전송됨.
관리자가 Pull requests 확인 후 merge하면 반영이 됨.

//0324 수업
이번주는 코드스페이스도 활용해서 충돌 해결(pull, push 단계 추가)
기능/버그 issues에 대한 갯수와 PR 오류 해결 비율이 있음 최소한 4:1 if) issues를 6개 제시하면 PR이 최소2개 이상이어야 6점 인정. 1개이면 4점까지만 인정

//0331 수업

Docker
devcontainer 깃헙에서 지향하는 개발환경을 Docker로 설정하기 위한 방식

REST API
GraphQL
REST API에서 일어나는 underfecting, overfecting 을 해결해줌.
ocktokit
깃헙 서버로부터 데이터를 가져올 수 있게 지원하는 공식 라이브러리
-REST API로도 요청 가능(작년에 이 방식으로 진행됨)
-GraphQL로도 요청 가능(올해는 같은 기능을 이 방식으로 진행)

C#, Typescript (자바스크립트에 정적타입을 끼얹은 녀석), Python 세 가지 언어 중에 Ocktokit이 지원되는 언어는 
C#, Typesript/Javascript 이고 Python은 지원되지 않음
Pyhon의 경우에는 직접 REST API라이브러리 또는 GraphQL라이브러리를 활용해야 함.

초반에 필요한 문서 이슈
-프로젝트 진행 규칙 정리
  -작업은 문서1개 + 버그/기능 1개 씩만 동시에 진행 가능 (PR 요청하면 다른거 진행 가능)
  -이슈 올라온지 24시간 이내로 자원자가 없으면 이슈 작성자가 댓글달고 작업 선점 가능
  -문서는 댓글로 작업 선점 후 24시간 이내, 버그/기능은 48시간 이내 PR을 넣지 않으면 다른 사람이 댓글 달고 작업 선점 가능
-점수 공식 (README에다가 추가)
-codespace가 아닌 windows에 vscode와 docker를 설치해서 코드스페이스와 같은 devcontainer환경을 구성하는 설치방법 안내
-C# 개발 환경에 대한 기본적인 안내
  - 컴파일러 포함한 기본적으로 설치해야 하는 것들 뭔지 / 설치 후 어떤 명령어로...
  -일반적으로 C# 프로젝트와 라이브러리 패키지를 관리하는 방법
  - vscode 에디터에서 C# 개발을 돕는 extension 확장은 어떤 걸 설치해야 하고 / 설치 후 기본적 사용법
-라이브러리 설치/활용 관련
  -Ocktokit 기본적인 활용 안내 (우리 프로젝트에 필요할 거 같은 거 위주)
  -GraphQL 기본적인 활용 안내 (")
  -CLI 프로그램 기본 개념/용어 및
   CLI관련 기능(옵션 처리 등등) 구현을 위한 라이브러리에 대한 문서화

개발환경 설정
-devcontainer를 어떻게 어떤 항목을 설치할지
-C#으로 터미널에 실행되는 CLI를 만들기 위한 라이브러리





